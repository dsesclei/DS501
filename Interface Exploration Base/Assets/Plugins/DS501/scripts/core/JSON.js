#pragma strict
/*
//TODO: convert to C#?
class JSON
{

	static function read_data( filename )
	{
		var in_file : StreamReader;
		in_file = new File.OpenText( filename );
		var data_JSON = in_file.ReadToEnd( );
		in_file.Close();
		
		return Json.Deserialize( data_JSON ) as Dictionary.<String,System.Object>;
	}
}
*/